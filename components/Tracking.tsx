'use client';
import {useEffect,useState} from 'react';
import {marketingAllowed,track} from '@/lib/tracking';
export function Tracking(){const [ask,setAsk]=useState(false);useEffect(()=>{let configured=false;function setup(){if(configured||!marketingAllowed())return;configured=true;const id=process.env.NEXT_PUBLIC_META_PIXEL_ID;if(id&&/^\d+$/.test(id)){if(!window.fbq){const f:NonNullable<Window['fbq']>=function(...args){if(f.callMethod)f.callMethod(...args);else f.queue?.push(args)};f.queue=[];f.loaded=true;f.version='2.0';window.fbq=f;const s=document.createElement('script');s.async=true;s.src='https://connect.facebook.net/en_US/fbevents.js';document.head.appendChild(s);window.fbq('init',id);}track('view_page');}}
try{setAsk(!localStorage.getItem('sala404-marketing'))}catch{}setup();window.addEventListener('sala404-consent',setup);const click=(e:MouseEvent)=>{if((e.target as Element).closest('[data-track="click_enter"]'))track('click_enter')};document.addEventListener('click',click);return()=>{window.removeEventListener('sala404-consent',setup);document.removeEventListener('click',click)}},[]);
function choose(yes:boolean){try{localStorage.setItem('sala404-marketing',yes?'yes':'no')}catch{}setAsk(false);window.dispatchEvent(new Event('sala404-consent'));}return ask?<aside className="privacy-choice" aria-label="Preferências de privacidade"><p>Podemos usar cookies da Meta para medir visitas e cadastros? Você pode entrar na sala mesmo sem aceitar. <a href="/privacidade"><u>Privacidade</u></a></p><div><button onClick={()=>choose(true)}>Aceitar</button><button onClick={()=>choose(false)}>Só essenciais</button></div></aside>:null;}
